import React from 'react';
import Card from 'react-bootstrap/Card';

const Usage = () => {
  return (
    <div>
      <Card className="usage">
        <h4>Use this web tool to obtain relative information for each fee.</h4>
      </Card>
      <hr></hr>
    </div>
  );
};

export default Usage;
